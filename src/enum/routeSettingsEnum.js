// Add routes to this object following the same pattern as below, App will loop through this and dynamically generate routes when called.
const routeSettings = {
    HOME: {
        path: '/',
        name: 'Container',
        RouteComponent: 'container/Container',
    },
};

export default routeSettings;
